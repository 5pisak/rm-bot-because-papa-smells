const axios = require('axios')

module.exports.getUserByDiscordID = (userid, action) => {
    if (!userid) return console.log('No userid provided.')
    axios.get(`https://api.blox.link/v1/user/${userid}`).then((res) => {
        const primaryAccount = res.primaryAccount
    })
}